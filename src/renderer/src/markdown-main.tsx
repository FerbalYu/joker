import React from 'react'
import ReactDOM from 'react-dom/client'
import MarkdownWindow from './markdown-window'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MarkdownWindow />
  </React.StrictMode>
)
